"""

    SimulateSystems(N)

Automatically simulate many time-series models at once and return a dataframe for further usage.

    Usage:
```julia-repl
SimulateSystems(N)
```
Arguments:
- `N` : The number of systems to simulate for each system type.
"""
function SimulateSystems(N::Int64 = 10)
    
    xx
end